fix all roles/tasks missing ""
fix ros_flush to not include duplicatred .id and fail, also exclude dynamic items/.id (update README for custom query var)
add ros_int_vlan role
add ros_int_eoip role
add ros_int_list role
add ros_ppp 
add ros_int_ovpn
add ros_int_sstp
add ros_ip_dns
add ros_ip_cloud
add ros_snmp
add ros_ip_neighbor_discovery
add ros_caps_man
add ros_ip_route
add ros7_ip_route 
add ros7_routing_filter 
add ros7_routing_ospf 
add ros_sys_script 

add ros_sys_scheduler 
add ros7_int_wireguard

IN PROG:
README's 

TODO:
release

